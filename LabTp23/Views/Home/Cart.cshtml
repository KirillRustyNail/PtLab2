<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Корзина</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 20px;
            background-color: #f8f8f8;
        }

        div {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: auto;
        }

        h3 {
            color: #333;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: center;
        }

        td p {
            margin: 0;
        }

        a {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div>
        <h3>Корзина</h3>
        <table>
            <tr>
                <td><p>Наименование</p></td>
            </tr>
            @foreach (var item in ViewBag.Cart.Products)
            {
                <tr>
                    <td><p>@item.Name</p></td>
                </tr>
            }
            <tr>
                <td><p>Общая стоимость: @ViewBag.Cart.TotalCost</p></td>
                <td><p><a href="/Home/Buy">Купить</a></p></td>
            </tr>
        </table>
    </div>
</body>
</html>
